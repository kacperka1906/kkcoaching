---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
import site from '../../data/site.pl.json';

const testimonials = (await getCollection('testimonials')).filter(t => t.data.consentOnFile);
const faqs = (await getCollection('faq')).sort((a, b) => a.data.order - b.data.order);
const filters = ['Wszystkie', 'Redukcja tkanki tłuszczowej', 'Mięśnie i siła', 'Pewność siebie i technika', 'Coaching online', 'Trening personalny'];
---
<BaseLayout
  title="Prawdziwe Przemiany Klientów | KK Coaching"
  description="Prawdziwe przemiany sylwetkowe klientów KK Coaching w Cwmbran i online. Prawdziwi ludzie, prawdziwa praca, prawdziwe efekty."
  lang="pl"
  path="/pl/transformations"
  altPath="/transformations"
>
  <section class="page-hero on-dark">
    <div class="container">
      <span class="eyebrow">{site.transformations.eyebrow}</span>
      <h1>{site.transformations.heading}</h1>
      <p class="lead">{site.transformations.body}</p>
    </div>
  </section>

  <section class="on-white">
    <div class="container">
      <div class="filters" role="tablist" aria-label="Filtruj przemiany według kategorii">
        {filters.map((f, i) => (
          <button class="filter-btn" class:list={{ active: i === 0 }} data-filter={f}>{f}</button>
        ))}
      </div>

      {testimonials.length === 0 && (
        <p class="empty-state">Brak opublikowanych przemian &mdash; dodaj wpisy w <code>src/content/testimonials/</code> dopiero po uzyskaniu pisemnej zgody klienta.</p>
      )}

      <div class="transform-grid">
        {testimonials.map(t => (
          <article class="transform-card">
            <img src={t.data.image} alt={t.data.imageAlt} width="400" height="300" loading="lazy" />
            <div class="transform-body">
              <div class="meta">
                <span>{t.data.name}</span>
                <span>{t.data.serviceType}</span>
                <span>{t.data.duration}</span>
              </div>
              <blockquote>{t.data.startingPoint} &rarr; {t.data.result}</blockquote>
            </div>
          </article>
        ))}
      </div>
    </div>
  </section>

  <section aria-labelledby="faq-heading">
    <div class="container">
      <div class="section-head centered">
        <span class="eyebrow">Pytania</span>
        <h2 id="faq-heading">Najczęściej zadawane pytania</h2>
      </div>
      <div class="faq-list">
        {faqs.map(async (item) => {
          const { Content } = await item.render();
          return (
            <details class="faq-item">
              <summary>{item.data.question}</summary>
              <div class="faq-answer"><Content /></div>
            </details>
          );
        })}
      </div>
      <p class="faq-note">Uwaga: treść FAQ jest obecnie w wersji angielskiej &mdash; przetłumacz pliki w <code>src/content/faq/</code> lub dodaj wersje <code>pl/</code>, tak jak przy usługach.</p>
    </div>
  </section>

  <section class="final-cta">
    <div class="container">
      <h2>{site.finalCta.heading}</h2>
      <p>{site.finalCta.body}</p>
      <a href="/pl/contact" class="btn btn-secondary">{site.finalCta.cta}</a>
    </div>
  </section>
</BaseLayout>

<script>
  const buttons = document.querySelectorAll('.filter-btn');
  const cards = document.querySelectorAll('.transform-card');
  buttons.forEach(btn => {
    btn.addEventListener('click', () => {
      buttons.forEach(b => b.classList.remove('active'));
      btn.classList.add('active');
      const filter = btn.getAttribute('data-filter');
      cards.forEach(card => {
        const type = card.querySelector('.meta span:nth-child(2)')?.textContent ?? '';
        const show = filter === 'Wszystkie' || type.toLowerCase().includes((filter ?? '').toLowerCase());
        (card as HTMLElement).style.display = show ? '' : 'none';
      });
    });
  });
</script>

<style>
  .page-hero { padding: 64px 0; }
  .page-hero h1 { font-size: clamp(1.8rem, 3.6vw, 2.6rem); margin: 12px 0 14px; }
  .page-hero .lead { color: #C7CBD1; max-width: 60ch; }
  .filters { display: flex; gap: 10px; flex-wrap: wrap; margin-bottom: 36px; }
  .filter-btn { background: var(--white); border: 1px solid #D8DBDE; border-radius: 999px; padding: 8px 16px; font-size: 0.85rem; cursor: pointer; color: var(--steel); }
  .filter-btn.active { background: var(--graphite); color: var(--white); border-color: var(--graphite); }
  .empty-state { color: var(--steel); font-style: italic; }
  .transform-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 24px; }
  @media (max-width: 700px) { .transform-grid { grid-template-columns: 1fr; } }
  .transform-card { background: var(--white); border-radius: var(--radius); overflow: hidden; border: 1px solid #E3E5E8; }
  .transform-card img { width: 100%; aspect-ratio: 4/3; object-fit: cover; }
  .transform-body { padding: 22px; }
  .meta { display: flex; gap: 14px; font-size: 0.8rem; color: var(--steel); margin-bottom: 10px; flex-wrap: wrap; }
  .transform-card blockquote { font-style: italic; border-left: 3px solid var(--red); padding-left: 14px; }
  .faq-list { max-width: 760px; margin: 0 auto; }
  .faq-item { border-bottom: 1px solid #D8DBDE; padding: 18px 0; }
  .faq-item summary { font-weight: 700; cursor: pointer; list-style: none; }
  .faq-item summary::-webkit-details-marker { display: none; }
  .faq-item summary::after { content: '+'; float: right; color: var(--red); }
  .faq-item[open] summary::after { content: '\2212'; }
  .faq-answer { color: var(--steel); margin-top: 10px; font-size: 0.95rem; }
  .faq-note { text-align: center; color: var(--steel); font-size: 0.82rem; margin-top: 24px; }
  .final-cta { background: var(--red); color: var(--white); text-align: center; padding: 96px 0; }
  .final-cta h2 { color: var(--white); }
  .final-cta p { color: #F3D7DA; max-width: 52ch; margin: 0 auto 32px; font-size: 1.05rem; }
</style>
